/* components */
import SplashScreen from '@/components/@common/SplashScreen';

const SplashGreenhousePage = () => {
  return <SplashScreen text={'온실로 이동중'} routerPath={'/greenhouse'} imgSrc={'greenhouse'} />;
};

export default SplashGreenhousePage;
